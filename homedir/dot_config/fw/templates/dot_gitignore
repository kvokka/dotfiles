# Secrets and local environment (never commit these)
.env
.env.*
!.env.example

# Logs
*.log
logs/

# Build artifacts (extend for the project's stack)
dist/
build/
coverage/
__pycache__/
*.py[cod]
.venv/
venv/

# ntm runtime state, logs and scout briefs (.ntm/briefs/). `.ntm/*` rather than
# `.ntm/`, so a repository can still commit its ntm config: `!.ntm/config.toml`.
.ntm/*
