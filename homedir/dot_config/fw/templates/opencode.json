{
  "$schema": "https://opencode.ai/config.json",
  // Added to the user's plugins: cc-safety-net, OpenCode's own destructive
  // command guard, next to dcg's plugin in .opencode/plugins/dcg-guard.js.
  "plugin": [
    "cc-safety-net@1.0.6"
  ]
}
