# ru (repo_updater), managed by chezmoi. Repository lists: ~/.config/ru/repos.d/*.txt
PROJECTS_DIR={{ .chezmoi.homeDir }}/proj/active
LAYOUT=flat
UPDATE_STRATEGY=ff-only
AUTOSTASH=false
PARALLEL=4
TIMEOUT=30
CHECK_UPDATES=false
