## Criteria

<!-- The originating acceptance: what must be observably true for the
     requester. One entry per criterion, in the requester's terms.

     Identifiers are stable. Never renumber, reuse or delete one; a superseded
     criterion stays here with its recorded outcome. Tick a box only with
     evidence that establishes the whole criterion. -->

### AC1: <observable outcome>

- [ ] Verified
- **Evidence**: <command and observed result, or a revision to open>
- **Notes**: <limitations, quirks or alternatives found while delivering
  this criterion - appended, never a rewrite of the criterion itself>

## Upstream criteria

<!-- When this change answers part of a larger request, link the criteria it
     serves instead of copying them. Delete this section if there are none. -->

## Notes

<!-- Findings that apply across criteria: what stayed unverified and why,
     alternatives recorded without a blocker, questions raised with the
     requester. -->

<!-- Artifacts open at heading level two on purpose: OpenSpec parses the
     level-two sections, and a level-one heading is taken as the change
     title instead. Markdown linters expect a top-level heading, so this
     file opts out of that one rule. -->
<!-- markdownlint-disable-file MD041 -->
