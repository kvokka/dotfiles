# cass-memory store: the playbook, diaries and traumas are the history worth
# keeping (reflections/ lists the sessions already processed, so a restored
# store does not reflect them again); logs, caches and the chezmoi-managed
# config are not.
/config.yaml
/.llm-cwd/
/.keep
/.cm-reflect.stderr
*.jsonl
!/traumas.jsonl
/cost/
/embeddings/
/onboarding-state.json
*.lock
*.lock.d/
/*.tmp
